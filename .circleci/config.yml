---
version: 2.1

executors:
  base:
    docker:
      - image: alpine/k8s:1.22.15

jobs:
  helm-lint:
      executor: base
      steps:
        - checkout
        - run: 
            command: |
              helm lint k8s/chart

workflows:
  build-1:
    jobs:
      - helm-lint